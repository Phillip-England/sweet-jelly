{
  "name": "cfasuite",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "dev": "npx nodemon --exec go run main.go --signal SIGTERM",
    "spill": "gofmt ./ >> Bundle.go",
    "tailwind": "tailwind -i './public/css/input.css' -o './public/css/output.css' --watch"
  },
  "dependencies": {
    "nodemon": "^3.0.2"
  }
}